{
  "name": "brandsitter",
  "version": "1.0.0",
  "description": "Brandsitter website",
  "scripts": {
    "start": "node proxy-server.js",
    "dev": "node proxy-server.js",
    "serve": "live-server --port=5500 --host=localhost --cors"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
